name: it-tools
services:
  it-tools:
    container_name: it-tools
    restart: unless-stopped
    ports:
      - 8080:80
    image: corentinth/it-tools:latest
    environment:
      - TZ=Asia/Bangkok
